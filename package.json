{
  "name": "ai-customer-support-agent",
  "version": "1.0.0",
  "description": "Root orchestrator for AI Customer Support Agent",
  "scripts": {
    "postinstall": "cd client && npm install && cd ../server && npm install && npx prisma generate",
    "build": "cd server && npx prisma generate && cd ../client && npm run build",
    "vercel-build": "npm run build"
  },
  "devDependencies": {
    "prisma": "^6.19.2"
  }
}
